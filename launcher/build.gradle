// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

apply plugin: 'com.android.application'
apply plugin: 'com.morechili.yuki.mergesdk'

dependencies {
    implementation project(':unityLibrary')
    implementation project(':unityLibrary:sdkmerge_combine.androidlib')
    implementation "com.morechili.yuki:mergesdk:6000.0.0"
    implementation "com.android.billingclient:billing-ktx:6.1.0"
}

android {
    namespace 'com.sdkmerge.demo'
    compileSdk 35
    buildToolsVersion '35.0.0'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
//    packagingOptions {
//        doNotStrip "*/*/libmimo_1011.so"
//        doNotStrip "*/*/libzeusLib.so"
//    }
    defaultConfig {
        minSdk 26
        targetSdk 33
        applicationId 'com.sdkmerge.demo'
        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64'
        }
        versionCode 1
        versionName '1.0'
    }

    androidResources {
        noCompress = ['.ress', '.resource', '.obb'] + unityStreamingAssets.tokenize(', ')
        ignoreAssetsPattern = "!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~"
    }

    signingConfigs {
        release {
            storeFile file('C:/Users/GM19/Desktop/keyStore/keystore/keystore/qudao.keystore')
            storePassword 'Aa123456'
            keyAlias 'game.keystore'
            keyPassword 'Aa123456'
        }
    }

    lint {
        abortOnError false
    }

    buildTypes {
        debug {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt')
            signingConfig signingConfigs.release
            jniDebuggable false
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt')
            signingConfig signingConfigs.release
        }
    }

    packaging {
        jniLibs {
                useLegacyPackaging true
        }
    }

    bundle {
        language {
            enableSplit = false
        }
        density {
            enableSplit = false
        }
        abi {
            enableSplit = true
        }
    }
    buildFeatures{
        buildConfig true
    }
//    configurations.configureEach {
//        resolutionStrategy {
//            force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.10'
//        }
//    }
//    //解决AAPT
//    aaptOptions.cruncherEnabled = false
//    aaptOptions.useNewCruncher = false
}
